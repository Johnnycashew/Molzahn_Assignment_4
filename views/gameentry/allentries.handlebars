{{#each games}}

    <div class="card card-body">
        <h4>{{title}}</h4>
        <p>{{price}}</p>
        <p>{{description}}</p>    
    </div>

{{else}}

<p>There are no games in the database.</p>

{{/each}}
